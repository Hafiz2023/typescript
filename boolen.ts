var age = 26;

if (age){
    console.log(" in if Age =" + age);
    
}
else {

    console.log("In if Age =" + age);
    
}

// Number to Boolean

    
    
var name2 = "ADIL";

if (name2){
    console.log(" in if anme =" + name2);
    
}
else {

    console.log("In if name =" + name2);
    
}
// Value conversion to boolean

// 1. Value conversion also works with logical operator && and || 
// 2.For &&

// a. if all values are converted to true , it will returns last valvalue 
// b. if any of the value converted to false it will that value.

// 3. FOR ||

   // A. If all value are converted to false , it will returns last value .
   // b. If any value converted to true it will return value.

// 
